@model GitHubExplorer.Web.Models.GitHubProfileModel
@{
    ViewBag.Title = "Search User";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>Search User</h2>
<div class="row">
    <div class="col-md-4">
        @using (Html.BeginForm("SearchUser", "GitHub"))
        {
            @Html.TextBoxFor(x => x.UserName)
            <span class="input-group-btn">
                <button class="btn btn-default">Search!</button>
            </span>
            <div>
                @Html.ValidationMessageFor(m => m.UserName, "", new { @class = "text-danger" })
            </div>
        }
    </div>
</div>
@if (!string.IsNullOrEmpty(Model.AvatarUrl))
{
    <hr />
    <div class="row">
        <div class="col-md-4">
            <h1>
                @Model.UserName
            </h1>
            <p>
                <img src="@Model.AvatarUrl" alt="@Model.UserName" />
            </p>
            <p>@Model.Location</p>
            <h3>Top 5 repositories</h3>
            <ul>
                @foreach (var repo in Model.GitHubRepositories)
                {
                    <li><a target="_blank" href="@repo.RepositoryUrl ">@repo.RepositoryName (stars : @repo.StarCount)</a></li>
                }
            </ul>
        </div>
    </div>
}
